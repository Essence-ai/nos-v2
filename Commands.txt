Archinstall

disk config: 
partitioning: use a best-effort default partitioning layout 
filesystem xfs

bootloader grub


authentication: 
add a user:
hostname: archlinux
password: password 
Should 'archlinux' be a superuser(sudo)?
yes

network config: 
copt iso network config to installation 

install 

--- When install is done ---

sudo reboot 

--- Delete ISO from VM ---


archlinux
password 

sudo pacman -S --needed archiso dos2unix qemu-full git base-devel python libvirt-python
git clone https://github.com/Essence-ai/nos-v2

cd ~/nos-v2

chmod +x setup.sh

./setup.sh --full
sudo ./setup.sh --iso


Shared Folder:

sudo mount -t vboxsf shared /mnt 
cp ~/nos-v2/out/*.iso /mnt 



---end---

Start UP: 

sudo systemctl start gdm


optional:
    qemu-system-x86_64 -enable-kvm -m 4G -nographic -serial mon:stdio -cdrom out/neuronos-*.iso

File System:
    lsblk
    lsblk -f 

    df -h

    sudo fdisk -l 


Delete Folder:
    sudo rm -r NeuronOS
    sudo rm -rf NeuronOS (FORCE DELETE!)


