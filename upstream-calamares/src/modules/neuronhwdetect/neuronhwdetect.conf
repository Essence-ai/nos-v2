# NeuronOS Hardware Detection Module Configuration
---
# Enable verbose logging during detection
verbose: true

# Write configuration files for VFIO
write_vfio_config: true

# Write hardware profile JSON for other NeuronOS components
write_profile: true

# Fail installation if no passthrough capability
# (set to false to allow installation without GPU passthrough)
require_passthrough: false
